import SVGMultiGame from '../../../components/SVG/SVGMultiGame';
import ButtoonFooter from '../ButtonFooter';

const ButtonMultiGame = () => {
    return <ButtoonFooter label="Multi Game" icon={<SVGMultiGame />} />;
};

export default ButtonMultiGame;
